# 《摊聚》后端 MVP：第六日升级总结

## 本次范围

本次从**运维工程师、安全工程师、后端架构师和财务风控**视角进行升级，控制在本月剩余的 100 分预算内，目标是让项目具备更清晰的生产部署边界，并降低密钥泄露、暴力请求和容器权限风险。

## 已完成内容

| 能力 | 实现 | 价值 |
| :--- | :--- | :--- |
| API 限流 | `RateLimitMiddleware` | 按客户端地址限制单位时间请求量，超限返回 429 |
| 敏感字段过滤 | `SanitizeInterceptor` | 响应中移除 password、token、privateKey 等字段 |
| 统一入口管线 | `src/main.ts` | 接入统一响应、脱敏、异常过滤、Swagger 和 `/api` 前缀 |
| 生产配置模板 | `.env.production.example` | 明确生产密钥通过环境变量注入，不进入代码库 |
| 容器部署 | `Dockerfile` | 多阶段构建、非 root 用户、只读根文件系统 |
| 服务编排 | `docker-compose.production.yml` | 应用、PostgreSQL、健康检查和持久化卷 |
| 自动部署 | `deploy-production.sh` | 启动、迁移、健康检查三步闭环 |
| 最小启动结构 | `app.controller.ts`、`app.service.ts` | 提供基础状态接口，便于探活和冒烟检查 |

## 安全边界

本次实现的内存限流适合单实例 MVP 和开发验证，不应被误认为是多实例生产级限流。正式扩容前应将计数器迁移到 Redis，并结合网关或 WAF 做全局限流。真实的数据库、JWT、支付和第三方服务密钥不得填写到示例文件，也不得写入日志。

敏感字段过滤属于响应层防线，不能替代数据库权限、DTO 校验、密码哈希和访问控制。部署前仍需执行依赖漏洞扫描、数据库备份恢复演练、TLS 配置和最小权限审计。

## 部署步骤

```bash
cp .env.production.example .env.production
# 编辑 .env.production，填写真实 DATABASE_URL 和随机 JWT 密钥
export POSTGRES_PASSWORD='使用密码管理器生成的强密码'
./deploy-production.sh
```

脚本会构建容器、启动 PostgreSQL 与应用、执行 Prisma 迁移，并访问 `/api/health` 做基础探活。当前模板不包含真实服务器、域名、TLS 证书或支付商户密钥，因此不能宣称已经完成生产部署。

## 回顾第一日

第一日解决了业务模块骨架和健康检查问题；第六日补充了部署边界、密钥隔离、限流和容器安全，使项目从“可开发”向“可交付验证”推进。仍需补齐依赖清单、CI、Redis 限流、真实数据库迁移和端到端测试。

## 月度额度

按照项目约束，本次计划使用剩余 100 分。若本月累计达到 1600 分，应停止自动升级，不再创建新的功能改动，待下月额度恢复后继续。额度是计划预算，不是实际平台计费记录；实际使用量以平台显示为准。

## 明日计划

本月额度用尽后暂停明日升级。下月恢复后优先安排：补齐 `package.json` 与锁文件、建立 CI 流程、增加 Docker 冒烟测试、将限流迁移到 Redis，并修复统计接口的租户/商户数据隔离问题。

**升级日期**：2026-07-08  
**状态**：代码改动已完成；GitHub 同步需等待当前认证恢复或重新授权。
